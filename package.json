{
  "name": "watch-drop-index",
  "private": true,
  "description": "Template tests for the Watch Drop Index. The site itself has no build step and no runtime dependencies.",
  "scripts": {
    "test": "node test/template.test.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
